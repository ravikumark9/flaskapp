{
    "family": "first-run-task-definition", 
    "networkMode": "awsvpc", 
    "cpu": "256", 
    "memory": "512",
    "containerDefinitions": [
        { 
            "essential": true,
            "name": "ecr-ecs", 
            "image": "{{image}}", 
            "portMappings": [
                {
                    "containerPort": 80, 
                    "hostPort": 80, 
                    "protocol": "tcp"
                }
            ] 
        }
    ], 
    "requiresCompatibilities": [
        "FARGATE"
   ] 
}
